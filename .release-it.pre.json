{
  "github": {
    "release": false,
    "tokenRef": "GH_TOKEN"
  },
  "hooks": {
    "after:bump": "version_string=$(echo \"${version}\" | sed -E 's/([0-9]+\\.[0-9]+\\.[0-9]+).*$/\\1/') && yarn react-native-version --never-amend && git checkout -b release/$version_string",
    "after:release": "version_string=$(echo \"${version}\" | sed -E 's/([0-9]+\\.[0-9]+\\.[0-9]+).*$/\\1/') && gh pr create --base develop --head release/$version_string --title 'Release ${version}' --body 'Release ${version}'"
  },
  "git": {
    "push": true,
    "commitMessage": "chore: Release v${version}",
    "tag": false
  }
}
